pub fn reference_pattern(x: &i32, y: &mut i32) -> i32 {
  let mut out = 0;

  match x {
    &a => out = a,
    _ => out = 0,
  }

  match y {
    &mut b => out = out + b,
    _ => out = out,
  }

  out
}
