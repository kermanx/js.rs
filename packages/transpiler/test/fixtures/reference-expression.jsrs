fn swap(a: &mut i32, b: &mut i32) {
  let a = effect1() + {
    if *a > *b {
      effect2()
    } else {
      return 1;
    }
  } + effect3();

  let tmp = *a;
  *a = *b;
  *b = tmp;
}

pub fn main() {
  let mut a = 1;
  let mut b = 2;
  swap(&mut a, &mut b);
}
