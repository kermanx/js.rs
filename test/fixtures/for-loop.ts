//pub fn for_loop() -> number {
export function for_loop(): number {
//  let mut acc = 0;
  let acc = 0;
//  for x in [1, 2, 3] {
  for (const x of [1, 2, 3]) {
//    acc = acc + x;
    acc = acc + x;
//  }
  }
//       acc
  return acc
//}
}

