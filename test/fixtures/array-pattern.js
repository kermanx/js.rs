import * as _r from "@jsrs/runtime";
export function array_pattern(arr) {
  const _t0 = arr;
  if (
    _t0.length >= 4 &&
    _t0[0] === 1 &&
    (a = _t0[1]) &&
    (b = _t0.at(-2)) &&
    true
  ) {
    var a, b;
    log(a, b);
  } else {
    log("other");
  }
}
