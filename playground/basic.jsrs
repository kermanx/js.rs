// pub use crate::x::{x::y::{self, y1, y2}, z::self as t, t as h};
// pub use t::x;
// pub use s::s;
// pub use r;

1 + 1;
let mut a = 1;
a.toString;

fn foo() -> number {
  let x = {
    return "1";
    1
  } + {
    2
  };
  x
}
